alias npm=npq-hero
alias wtf3000="lsof -i tcp:3000"

# fzf key bindings loaded from ~/.fzf.zsh:
#   Ctrl-T: fuzzy-find files/directories and insert the selection into the command line.
#   Alt-C: fuzzy-find directories and cd into the selected directory.
alias ls="eza --icons -lah --git --group-directories-first"
alias ll="eza -lh --icons --grid"
alias cat=bat
